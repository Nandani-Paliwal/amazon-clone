# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools delete kr .vs code aur pura prohject to aise ye kyu krti hai tu kii folder > folder > project , sidhaa folder > project file kyu nii 

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_CHALLENGE_22A1F }}'
          channelId: live
          projectId: challenge-22a1f
